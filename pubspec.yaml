name: bdb_challenge
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  bloc: ^8.1.0
  cached_network_image: ^3.2.2
  cloud_firestore: ^4.2.0
  cupertino_icons: ^1.0.2
  equatable: ^2.0.5
  firebase_core: ^2.3.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  flutter_blurhash: ^0.7.0
  hotels_repository:
    path: packages/hotels_repository
  image_cache_manager:
    path: packages/image_cache_manager
  
  video_player: ^2.4.8
 

dev_dependencies:
  bloc_test: ^9.1.0
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  mocktail_image_network: ^0.3.1
  very_good_analysis: ^3.1.0


flutter:
  uses-material-design: true
